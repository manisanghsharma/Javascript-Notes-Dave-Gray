let item = "Gulab Jamun";
// let item;
let reply;
if(item){
    reply = `Here is your order of ${item}!`;
}
else{
    reply = `${item} is out of stock. Sorry for the inconvenience`;
}

console.log(reply);

//`` - is used to add ${}, this key is besides 1